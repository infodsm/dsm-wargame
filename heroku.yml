setup:
 addons:
   - plan: heroku-postgresql
build:
 docker:
   web: ./Dockerfile
run:
   command:
     - gunicorn 'CTFd:create_app()'
   image: web
release:
   image: web
   command:
   - ./docker-entrypoint.sh
